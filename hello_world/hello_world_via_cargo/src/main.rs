/*
    use cargo to create project "cargo new hello_world_via_cargo (take snack case)"
    you can build using "cargo build" or release using "cargo build --release"
    you can run using "cargo run" to build and run in debug mode
    or you can run using "cargo run --release" to build and run in release mode
*/ 
fn main() {
    println!("Hello, world!");
}
